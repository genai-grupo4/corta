{
  "name": "corta",
  "version": "2.0.0",
  "description": "Acortador de URLs interno",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
